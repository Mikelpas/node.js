<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Dashboard</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://fonts.googleapis.com/css?family=Montserrat:600,700&display=swap" rel="stylesheet">
    <style>
        body {
            margin: 0;
            min-height: 100vh;
            font-family: 'Montserrat', sans-serif;
            background: linear-gradient(135deg, #f7971e 0%, #ffd200 100%);
            overflow-x: hidden;
        }
        .sidebar {
            position: fixed;
            left: 0; top: 0; bottom: 0;
            width: 220px;
            background: rgba(255,255,255,0.95);
            box-shadow: 2px 0 16px rgba(0,0,0,0.07);
            display: flex;
            flex-direction: column;
            align-items: center;
            padding-top: 2.5rem;
            z-index: 10;
        }
        .sidebar .logo {
            font-size: 20px;
            font-weight: bold;
            color: #f7971e;
            margin-bottom: 2.5rem;
            margin-left: 5px;
            letter-spacing: 2px;
            animation: logoPop 1s cubic-bezier(.68,-0.55,.27,1.55);
        }
        @keyframes logoPop {
            0% { transform: scale(0.7); opacity: 0; }
            80% { transform: scale(1.1); opacity: 1; }
            100% { transform: scale(1); }
        }
        .sidebar nav {
            width: 100%;
        }
        .sidebar nav a {
            display: block;
            padding: 1rem 2rem;
            color: #444;
            text-decoration: none;
            font-weight: 600;
            border-radius: 8px 0 0 8px;
            margin-bottom: 0.5rem;
            transition: background 0.2s, color 0.2s, transform 0.2s;
            position: relative;
        }
        .sidebar nav a.active, .sidebar nav a:hover {
            background: linear-gradient(90deg, #f7971e 60%, #ffd200 100%);
            color: #fff;
            transform: translateX(8px) scale(1.05);
        }
        .sidebar .logout-btn {
            margin-top: auto;
            margin-bottom: 2rem;
            background: linear-gradient(90deg, #f857a6 0%, #ff5858 100%);
            color: #fff;
            border: none;
            border-radius: 8px;
            padding: 0.7rem 2.5rem;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            box-shadow: 0 2px 8px rgba(248,87,166,0.13);
            transition: background 0.2s, transform 0.2s;
        }
        .sidebar .logout-btn:hover {
            background: linear-gradient(90deg, #ff5858 0%, #f857a6 100%);
            transform: scale(1.05);
        }
        .main-content {
            margin-left: 240px;
            padding: 2.5rem 3vw;
            min-height: 100vh;
        }
        .dashboard-header {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 2.5rem;
            animation: fadeInDown 1s;
        }
        @keyframes fadeInDown {
            from { opacity: 0; transform: translateY(-30px);}
            to { opacity: 1; transform: translateY(0);}
        }
        .dashboard-header .welcome {
            font-size: 2.1rem;
            font-weight: 700;
            color: #f7971e;
            letter-spacing: 1px;
        }
        .dashboard-header .user-info {
            display: flex;
            align-items: center;
            gap: 1.2rem;
        }
        .dashboard-header .avatar {
            width: 54px;
            height: 54px;
            border-radius: 50%;
            background: linear-gradient(135deg, #f7971e 60%, #ffd200 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            color: #fff;
            font-size: 1.7rem;
            font-weight: bold;
            box-shadow: 0 2px 8px rgba(0,0,0,0.10);
            animation: avatarPop 1.2s cubic-bezier(.68,-0.55,.27,1.55);
        }
        @keyframes avatarPop {
            0% { transform: scale(0.5); opacity: 0;}
            80% { transform: scale(1.1);}
            100% { transform: scale(1); opacity: 1;}
        }
        .dashboard-header .user-details {
            font-size: 1.1rem;
            color: #444;
        }
        .cards-row {
            display: flex;
            flex-wrap: wrap;
            gap: 2rem;
            margin-bottom: 2.5rem;
            justify-content: flex-start;
        }
        .dashboard-card {
            background: #fff;
            border-radius: 18px;
            box-shadow: 0 8px 32px rgba(0,0,0,0.10);
            padding: 2rem 2.2rem;
            min-width: 260px;
            flex: 1 1 260px;
            max-width: 340px;
            position: relative;
            overflow: hidden;
            cursor: pointer;
            transition: transform 0.25s cubic-bezier(.68,-0.55,.27,1.55), box-shadow 0.2s;
            animation: cardPop 0.8s cubic-bezier(.68,-0.55,.27,1.55);
        }
        .dashboard-card:hover {
            transform: translateY(-10px) scale(1.04) rotate(-1deg);
            box-shadow: 0 16px 48px rgba(0,0,0,0.13);
        }
        @keyframes cardPop {
            0% { transform: scale(0.7) translateY(40px); opacity: 0;}
            100% { transform: scale(1) translateY(0); opacity: 1;}
        }
        .dashboard-card .card-icon {
            font-size: 2.5rem;
            color: #ffd200;
            margin-bottom: 1.2rem;
            animation: iconSpin 2s infinite linear;
        }
        @keyframes iconSpin {
            0% { transform: rotate(0);}
            100% { transform: rotate(360deg);}
        }
        .dashboard-card .card-title {
            font-size: 1.2rem;
            font-weight: 700;
            color: #f7971e;
            margin-bottom: 0.7rem;
        }
        .dashboard-card .card-desc {
            color: #555;
            font-size: 1rem;
            margin-bottom: 1.2rem;
        }
        .dashboard-card .card-link {
            color: #f857a6;
            font-weight: 600;
            text-decoration: none;
            transition: color 0.2s;
        }
        .dashboard-card .card-link:hover {
            color: #ff5858;
            text-decoration: underline;
        }
        .activity-section {
            background: rgba(255,255,255,0.95);
            border-radius: 18px;
            box-shadow: 0 8px 32px rgba(0,0,0,0.08);
            padding: 2rem 2.2rem;
            margin-bottom: 2rem;
            animation: fadeInUp 1.2s;
        }
        @keyframes fadeInUp {
            from { opacity: 0; transform: translateY(40px);}
            to { opacity: 1; transform: translateY(0);}
        }
        .activity-section h3 {
            color: #f7971e;
            margin-bottom: 1.2rem;
            font-size: 1.3rem;
            font-weight: 700;
        }
        .activity-list {
            list-style: none;
            padding: 0;
            margin: 0;
        }
        .activity-list li {
            padding: 0.7rem 0;
            border-bottom: 1px solid #f3e5ab;
            color: #444;
            font-size: 1rem;
            transition: background 0.2s;
        }
        .activity-list li:last-child {
            border-bottom: none;
        }
        .activity-list li:hover {
            background: #fffbe6;
        }
        @media (max-width: 900px) {
            .main-content { margin-left: 0; padding: 1.2rem;}
            .sidebar { position: static; width: 100vw; flex-direction: row; height: 70px; padding-top: 0;}
            .sidebar .logo { margin-bottom: 0; font-size: 1.3rem;}
            .sidebar nav { display: flex; flex-direction: row; }
            .sidebar nav a { border-radius: 8px; margin: 0 0.5rem 0 0;}
            .sidebar .logout-btn { margin: 0 0 0 auto; padding: 0.5rem 1.2rem;}
        }
        @media (max-width: 700px) {
            .cards-row { flex-direction: column; gap: 1.2rem;}
            .dashboard-card { max-width: 100%;}
        }
    </style>
</head>
<body>
    <div class="sidebar">
        <span class="logo">MyDashboard</span>
        <nav>
            <a href="/dashboard" class="active">Dashboard</a>
            <a href="/signin">Signin</a>
            <a href="/signup">Signup</a>
        </nav>
        <form action="/logout" method="POST" style="width:100%;">
            <button class="logout-btn" type="submit">Logout</button>
        </form>
    </div>
    <div class="main-content">
        <div class="dashboard-header">
            <div class="welcome">
                Welcome<% if (typeof user !== 'undefined') { %>, <%= user.name %><% } %>!
            </div>
            <div class="user-info">
                <div class="avatar">
                    <% if (typeof user !== 'undefined' && user.avatar) { %>
                        <img src="<%= user.avatar %>" alt="avatar" style="width:100%;height:100%;border-radius:50%;">
                    <% } else if (typeof user !== 'undefined' && user.name) { %>
                        <%= user.name[0].toUpperCase() %>
                    <% } else { %>
                        <span>U</span>
                    <% } %>
                </div>
                <div class="user-details">
                    <% if (typeof user !== 'undefined') { %>
                        <div><strong><%= user.name %></strong></div>
                        <div style="font-size:0.95rem;color:#888;"><%= user.email %></div>
                    <% } %>
                </div>
            </div>
        </div>
        <div class="cards-row">
            <div class="dashboard-card" style="animation-delay:0.1s;">
                <div class="card-icon">📊</div>
                <div class="card-title">Analytics</div>
                <div class="card-desc">View your usage stats, trends, and insights in real time.</div>
                <a href="/analytics" class="card-link">Go to Analytics →</a>
            </div>
            <div class="dashboard-card" style="animation-delay:0.2s;">
                <div class="card-icon">📝</div>
                <div class="card-title">Tasks</div>
                <div class="card-desc">Manage your to-do list, set reminders, and track progress.</div>
                <a href="/tasks" class="card-link">Manage Tasks →</a>
            </div>
            <div class="dashboard-card" style="animation-delay:0.3s;">
                <div class="card-icon">💬</div>
                <div class="card-title">Messages</div>
                <div class="card-desc">Check your latest messages and notifications.</div>
                <a href="/messages" class="card-link">View Messages →</a>
            </div>
            <div class="dashboard-card" style="animation-delay:0.4s;">
                <div class="card-icon">⚙️</div>
                <div class="card-title">Settings</div>
                <div class="card-desc">Update your preferences and account settings.</div>
                <a href="/settings" class="card-link">Edit Settings →</a>
            </div>
        </div>
        <div class="activity-section">
            <h3>Recent Activity</h3>
            <ul class="activity-list">
                <li>Signed in from a new device <span style="float:right;color:#ffd200;">2 mins ago</span></li>
                <li>Updated profile information <span style="float:right;color:#ffd200;">1 hour ago</span></li>
                <li>Completed "Setup Account" task <span style="float:right;color:#ffd200;">Yesterday</span></li>
                <li>Received a new message <span style="float:right;color:#ffd200;">2 days ago</span></li>
            </ul>
        </div>
    </div>
</body>
</html>